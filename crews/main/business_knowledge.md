# 业务知识（business_knowledge）

> 这是 main agent（小贝）的核心业务知识文件，**单文件**，不是文件夹。
> 所有工作的出发点都基于此；待补充或不清晰的部分，由 main agent 在实践中与用户持续打磨。
> 落盘前必须征得用户同意——这份文件非常关键。
>
> 支撑材料（产品截图、价目表、案例、合同模板等）放同名文件夹 `business_knowledge/`，
> 与本文件互为参照；详见 `AGENTS.md` 开头说明。

> 📝 **模板说明**：以下内容未确定 / 不适用的段落保留占位，由 main agent 与用户在 BOOTSTRAP 及后续运营中逐步填实。
> 凡 ` <!-- 由main agent启用时填入并负责后续持续优化更新 -->` 处皆不要凭空编造。

---

## 1. 产品背景与定位

| 项目 | 内容 |
|------|------|
| **英文名称** |  <!-- 由main agent启用时填入并负责后续持续优化更新 --> |
| **中文名称** |  <!-- 由main agent启用时填入并负责后续持续优化更新 --> |
| **定位** |  <!-- 由main agent启用时填入并负责后续持续优化更新 --> |
| **目标受众** |  <!-- 由main agent启用时填入并负责后续持续优化更新 --> |
| **核心技术** |  <!-- 由main agent启用时填入并负责后续持续优化更新 --> |
| **主域名** |  <!-- 由main agent启用时填入并负责后续持续优化更新 --> |
| **一句话介绍** |  <!-- 由main agent启用时填入并负责后续持续优化更新 --> |

## 2. 产品简介 / 能力列表

 <!-- 由main agent启用时填入并负责后续持续优化更新 -->

## 3. 主营业务（三大板块）

 <!-- 由main agent启用时填入并负责后续持续优化更新 -->

### 3.1 收费项目与定价

 <!-- 由main agent启用时填入并负责后续持续优化更新 -->

### 3.2 关键合作 / 客户 / 资源

 <!-- 由main agent启用时填入并负责后续持续优化更新 -->

## 4 红线与注意事项

 <!-- 由main agent启用时填入并负责后续持续优化更新 -->

---

# 维护记录

> 每次实质更新在此追加一行（日期 + 改了什么 + 谁确认）。落盘前必须征得用户同意。

| 日期 | 变更 | 确认 |
|------|------|------|
| （模板初始化） | 由 main agent 基于 brand-info 模板创建 | — |
